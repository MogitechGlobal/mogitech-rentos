name: MogiRentOS CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install Dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate --schema=./apps/api/prisma/schema.prisma
        
      - name: Build Backend
        run: npm run build --workspace=apps/api
        
      - name: Build Frontend
        run: npm run build --workspace=apps/web

  # Optional: Push to AWS ECR (Uncomment when AWS is configured)
  # push_to_ecr:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - Configure AWS credentials, login to ECR, build Docker images, and push.